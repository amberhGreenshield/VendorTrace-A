# Fill in once you've registered the sign-in app (see src/lib/msalConfig.ts)
VITE_MSAL_CLIENT_ID=
VITE_MSAL_TENANT_ID=

# Once the frontend calls the real API instead of localStorage
VITE_API_BASE_URL=http://localhost:4000
